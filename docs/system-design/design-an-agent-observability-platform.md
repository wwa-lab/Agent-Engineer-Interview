# Design an Agent Observability Platform

Normalize run, model, retrieval, tool, policy, and outcome events around a trace ID. Store searchable metadata separately from sensitive payloads, support sampling and redaction, and join traces to evaluation and user feedback. Alert on tail latency, cost spikes, policy denials, tool error classes, and quality regressions—not only request failures.
