# LLM-as-a-Judge

Use a model judge for semantic criteria that exact assertions cannot cover, such as whether an explanation addresses the user’s constraints. Provide a rubric, blind irrelevant metadata, calibrate against human labels, monitor disagreement, and keep safety and schema checks deterministic where possible. Never let an unreviewed judge be the only release gate for high-impact behavior.
