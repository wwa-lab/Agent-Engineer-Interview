# Planning

Planning can decompose a task into steps, but plans are hypotheses. Validate dependencies before execution, cap plan length, and re-plan only when a tool observation changes the state. For simple or high-risk tasks, a fixed workflow may be safer. Measure whether planning improves success after its added latency and cost.
