# Observability

Observe runs as traces with step spans, model and prompt versions, retrieval metadata, tool calls, policy decisions, latency, token usage, errors, and outcome. Redact sensitive payloads, separate audit access, and define retention before shipping verbose traces.
