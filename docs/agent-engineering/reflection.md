# Reflection

Reflection is a second pass that critiques or verifies an output. It helps when a rubric exposes a recoverable error, such as a missing citation or invalid format. It can also add cost and produce confident rationalization. Trigger it with evidence, compare against a baseline, and keep deterministic checks first.
