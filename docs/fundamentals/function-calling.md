# Function Calling

Function calling is a contract among model, runtime, and tool. Register a narrow schema, validate arguments, authorize the action, execute with timeout/idempotency, and return a typed observation. Keep the model from directly receiving credentials or choosing a permission scope.
