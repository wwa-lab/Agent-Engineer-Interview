# Structured Output

Use schemas to make model decisions parseable, then validate values and business invariants in application code. Reject unknown tool names, out-of-range values, missing required fields, and actions outside the current policy. A valid JSON document can still request an unauthorized transfer.
