# Chunking

Chunk along semantic and access-control boundaries. Preserve headings, source IDs, timestamps, and document versions. Overlap can recover references but increases index size and duplicate context. Compare fixed, recursive, and structure-aware strategies on retrieval and answer metrics.
