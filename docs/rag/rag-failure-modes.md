# RAG Failure Modes

Common failures include bad parsing, chunk boundaries, stale indexes, permission leakage, empty retrieval, conflicting sources, context overflow, and citations that do not support the claim. Create one regression fixture per failure and define a safe response for each.
