# Memory

Separate ephemeral conversation state, task state, and durable user memory. Store the smallest useful fact with provenance, confidence, scope, expiry, correction, and deletion semantics. Retrieval from memory must respect tenant and user authorization; a vector index is not a privacy policy.
