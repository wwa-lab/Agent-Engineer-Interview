# Tracing

A trace should answer: what task ran, which context and versions were used, what actions occurred, where time and cost went, and why the run stopped. Use stable run/step IDs and correlation across queues and tools. Keep replay fixtures safer than raw production transcripts.
