# Beginner Projects

Build a cited document assistant, a read-only repository explainer, or a support-ticket classifier with structured output. Keep the first version deterministic around the model and add a small offline evaluation set. Avoid pretending a toy app is production-ready.
