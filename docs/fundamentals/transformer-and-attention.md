# Transformer and Attention

Transformers process token representations through attention and feed-forward layers. Attention lets a position weigh other positions in the context, which helps model relationships without a fixed sequence of rules. You do not need to derive every matrix in an interview; explain why long context has cost and why attention does not equal retrieval or memory.
