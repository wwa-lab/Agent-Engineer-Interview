# Reliability

Use timeouts, bounded retries, exponential backoff with jitter, circuit breakers, idempotency, durable state, and clear fallbacks. Classify errors before retrying. For long-running work, checkpoint state and let users cancel or resume. Reliability includes predictable refusal and escalation, not only availability.
